
# Para la clave con sudo
env:
  global:
    - USUARIO=root
    - CONTRASENA=''
sudo: required

language: python
python:
  - "2.7"

# Servicio mongo
services:
  -mongodb
# command to install dependencies
install: make install
# command to run tests
script: make test
